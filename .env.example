# -------- Litecoin Network selection --------
# One of: mainnet | testnet | regtest
# Defaults to regtest if omitted.
NETWORK=regtest

# -------- Mainchain RPC (Litecoin) --------
RPC_HOST=127.0.0.1

# RPC_PORT is OPTIONAL now:
# - mainnet default  = 9332
# - testnet default  = 19332
# - regtest default  = 19443
# If you set RPC_PORT here, it overrides the default for the chosen NETWORK.
# For your current setup (Litecoin regtest), you can either rely on the default:
#   NETWORK=regtest
#   (no RPC_PORT line)
# or keep it explicit as below:
RPC_PORT=19443

RPC_USER=rpcuser
RPC_PASSWORD=rpcpassword

# -------- Sidechain RPC --------
SC_RPC_HOST=127.0.0.1
SC_RPC_PORT=18554
SC_RPC_USER=scrpcuser
SC_RPC_PASSWORD=scrpcpassword

# Set to 0 to completely disable sidechain RPC usage and BMM OP_RETURN output
ENABLE_SIDECHAIN=1

# -------- Stratum Server --------
STRATUM_HOST=0.0.0.0
STRATUM_PORT=3333
JOB_REFRESH_INTERVAL=10
POOL_DIFFICULTY=0.1

# Max number of jobs to retain in memory before pruning (optional; not currently used)
MAX_JOBS_IN_MEMORY=1000

# -------- Drivechain --------
BMM_HEADER_HEX=D1617368
SIDECHAIN_NUMBER=0

# -------- Miner --------
# 20-byte pubkey hash (NOT the whole address).
# Leave empty to pay OP_TRUE (testing only; not safe on mainnet-like setups)
MINER_PKH_HEX=1d0f172a0ecb48aee1be1f2687d2963ae33f71a1
