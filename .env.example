# -------- Litecoin Network selection --------
# One of: mainnet | testnet | regtest
# Defaults to regtest if omitted.
NETWORK=regtest

# -------- Mainchain RPC (Litecoin) --------
RPC_HOST=127.0.0.1

# RPC_PORT is OPTIONAL:
# - mainnet default  = 9332
# - testnet default  = 19332
# - regtest default  = 19443
# You can comment this out if your node uses the default port.
RPC_PORT=19443

RPC_USER=rpcuser
RPC_PASSWORD=rpcpassword

# -------- Sidechain RPC --------
SC_RPC_HOST=127.0.0.1
SC_RPC_PORT=18554
SC_RPC_USER=scrpcuser
SC_RPC_PASSWORD=scrpcpassword

# Set to 0 if you are NOT actually running the sidechain daemon yet.
ENABLE_SIDECHAIN=1

# -------- Stratum Server --------
# Bind on all interfaces so the Antminer L3 on your LAN can connect.
STRATUM_HOST=0.0.0.0
STRATUM_PORT=3333

# How often to push a fresh job to miners (seconds)
JOB_REFRESH_INTERVAL=10

# Pool share difficulty (Stratum difficulty, *not* network nBits).
# 0.1 is fine for testing; with a real L3 you can go to 1.0 or higher later.
POOL_DIFFICULTY=0.1

# -------- Drivechain --------
BMM_HEADER_HEX=D1617368
SIDECHAIN_NUMBER=0

# -------- Miner payout --------
# 20-byte pubkey hash (NOT the whole address).
# This should be the "pubkeyhash" field from `getaddressinfo "your_LTC_address"`.
# For testing-only you can leave this as-is; on anything valuable, change it to
# a pubkeyhash you actually control.
MINER_PKH_HEX=1d0f172a0ecb48aee1be1f2687d2963ae33f71a1
